class Grid {
  constructor(numRows, numColumns, tileSpacing, tileDimension) {
    this.numRows = numRows;
    this.numColumns = numColumns;
    this.tileSpacing = tileSpacing;
    this.tileDimension = tileDimension;
  }
}
